import { Icon } from '@iconify/react';

const Next = () => {
	return (
		<button className="btn-action-icon">
			<span>
				<Icon icon="mdi:skip-next" />
			</span>
		</button>
	);
};

export default Next;
